# Локальные секреты (не коммитить)

Сюда перенесите вручную чувствительные файлы, например:

- `admin-credentials.txt` (если использовали в корне)

В `.gitignore` добавлено игнорирование содержимого, кроме этого README.
